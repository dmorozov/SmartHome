# TODO — needs you

Everything below needs **the owner specifically**: physical access to a device, a
credential or a payment, or a judgement call about your own house. Anything an agent
could do headlessly is deliberately **not** on this list.

Generated 2026-08-04 and **re-measured against the live host on 2026-08-04 ~18:30** —
every verdict below is a command that was run, not a re-read of the previous list.
[`appliance/COMMISSIONING.md`](appliance/COMMISSIONING.md) is the master runbook; this
is only the subset that needs a human. Phase plans live in
[`docs/plans/device-integrations/`](docs/plans/device-integrations/README.md) — not
restated here.

Item IDs (A1, B2, …) are stable handles, not an ordering. Done items are kept, not
deleted — the reasoning is the part worth being able to re-read.

**What changed in this pass:** **G4 is done** — `flutter build linux --release`
succeeds and the resulting binary has rendered live video from go2rtc, so three items
that were written around "the wall has never played a frame" are rewritten below
(**G4**, **A7**, **B2**). **G1 is done.** **G6 is new**: `cage` turns out not to be
installed, which is a prerequisite A7 did not have yesterday. Four descriptions were
factually wrong and are corrected: **B4** (the HACS installer has never run — this is
install *and* auth), **E4** ("the default is short" — it is measured at **10 minutes**),
**E7** (the `ring-mqtt-data/` half of the backup is currently empty), and **C1**
(whether the lease is reserved is *not* knowable from this host).

### Do these first

1. **B4** — HACS. Bigger than this list used to say — the installer has never been run, so it is install *and* GitHub auth. Gates **B6**, **B7**, **B8**.
2. **F1** — the drawing. Largest single unlock in the repo, and four other items are queued behind it — now five, since the entry and stairs lights are sitting on placeholder Keys that only F1 can name properly. Needs a real work session, so start it when you have one.
3. ~~**G3**~~ — done 2026-08-07: both the file-tidiness half (ADR-0010) and the credential rotation are closed. Nothing left on this list is a *live* exposure.

**This list turned over completely on 2026-08-04/05.** Everything that used to be
on it is done: **E7** (backup taken and moved off-box), **E4** (`manual_run_mins:
5`, verified in `.storage`), **A3** (EP40 socket mapping), and **B2** (Ring
authenticated — the Panel now plays a real picture of the front door). Each is
kept below with its reasoning rather than deleted.

**G3 is closed as of 2026-08-07** — both halves: the file consolidation
(ADR-0010) and the credential rotation. This paragraph used to point at it as
tonight's twenty-minute job; nothing on the list right now is a comparably
quick, comparably live item, so there isn't a standing replacement recommendation.

### At the hardware — physical access

- [x] **A3 · Which EP40 socket is `_0` and which is `_1` — DONE 2026-08-05.** `switch.outdoor_outlet_a` drives the socket physically labelled **"Plug 1"**; `switch.outdoor_outlet_b` is the other one. *Verified by:* the owner at the plug — toggled A from HA, watched that socket respond. Registry chain re-read the same day and it holds end to end: `switch.outdoor_outlet_a` → `unique_id 8006…F4F40F800` → `original_name "Kasa_Smart Plug_722C_0"`, and `_b` → `…F801` → `_1`. *The trap this closes, and it is a live one:* **TP-Link labels the sockets from 1, HA numbers the children from 0**, so "Plug 1" is child `_0` — anyone matching the digit on the plastic to the digit in the entity id gets both outlets backwards, and nothing on screen would show it. *One honest limit:* only A was toggled. B is the other socket **by elimination** — sound for a two-socket unit, but not the same evidence. *Also settled, as a side effect:* the ~30 min "rename chain" this entry warned about turns out to be already done — the entities have carried their final ids since 2026-08-04, `bindings.yaml` binds them, and `_integrated` in `bindings_drift_test.dart` lists both. Nothing was left to run. *Still open, and it is the other half of the round-trip:* pressing the plug's own button and watching the entity follow (Ch. 4 §4.1.7 step 2) — that is what proves the `state_changed` path the Panel depends on, and HA → plug alone does not. → [Ch. 4 §4.1.5](appliance/commissioning/04-devices-local.md) for the mapping table, [Ch. 7 §7.8](appliance/commissioning/07-device-lifecycle.md) for the rename as-built.
- [ ] **A2 · Get the Rachio's HomeKit setup code off the hardware** — an 8-digit `XXX-XX-XXX` code on the device body, the packaging, or in the Rachio app. *Why you:* it cannot be fetched, derived or reset from the Hub. *Time:* minutes, once the code is in hand. *Unblocks:* WAN-outage-resilient zone control; the cloud entry keeps working regardless. *Measured 2026-08-04:* the flow is still sitting there — `handler: homekit_controller`, **`step_id: "pair"`**, `unique_id: d3:fc:32:3a:79:5d`, `Rachio-BFF806`, category `Bridge`. Independently corroborated: `.storage/homekit_controller-entity-map` holds exactly **one** pairing (`61:CB:0F:63:96:36`, the Ecobee) and the Rachio HAP id is not in it. "The cloud entry keeps working regardless" is verified — the `rachio` entry is `loaded` with 10 entities including 5 zone switches. → [Ch. 4 §4.3.3](appliance/commissioning/04-devices-local.md) — steps for the HA side (type it **dashed**; undashed fails silently); *where the code is printed* is UNVERIFIED. If the code is genuinely lost, do **not** factory-reset to recover it — that invalidates the working cloud entry.
- [ ] **A4 · Run the Ecobee outage drill** — pull the Hub host's WAN (or blackhole `ecobee.com`) and prove the *local* entity keeps working while the cloud one goes `unavailable`. *Why you:* it interrupts the household's internet. *Time:* minutes. *No longer gated* — **B1 is done 2026-08-07**, and `climate.main_floor_2` now exists (verified via `config_entries/get`), so both entries the drill compares are in place. *Unblocks:* the only proof that `connectivity: local` on the `thermostat` pin is not a lie. *Measured 2026-08-04, now stale by construction — re-measure before running the drill:* at that time there was still exactly one climate entity, `climate.main_floor`, and no `climate.main_floor_2`. That has changed. → [Ch. 4 §4.2.6, "Verify — the outage drill"](appliance/commissioning/04-devices-local.md) — 5 numbered steps.
- [ ] **A5 · First deliberate Rachio zone test at the sprinkler head** — someone outside, watching water actually run. *Why you:* physical presence, at a time when a running zone is not a problem. *Time:* minutes. **Set E4 first — it is 10 minutes per tap until you do.** *Unblocks:* confidence that the 5 zone switches map to the valves the Rachio app names. → [Ch. 4 §4.3.5](appliance/commissioning/04-devices-local.md) — steps, including the correct attribute key (`Zone number`, capitalised, space-separated).
- [ ] **A1 · Re-commission the Tesla Wall Connector** — the unit at `.52` RSTs every TCP port and still advertises a `PROV_` mDNS record. Unblocking it runs through the Tesla One app against the charger's own AP, possibly a factory reset. *Why you:* physical access to the unit and the breaker, plus a Tesla account session. Nothing on the network side can move it. *Time:* ~15 min if the Quick Start Guide is to hand; a work session if it needs a reset. *Unblocks:* the `ev-charger` Key. Nothing else — phase 2 de-gated on it. *Measured 2026-08-04:* `192.168.68.52` answers ICMP but **refuses** 80 and 443 — the described symptom is unchanged. → [Ch. 4 §4.4.4 walkthrough → §4.4.5 re-probe → §4.4.6 add to HA](appliance/commissioning/04-devices-local.md). **Do §4.4.4 Step 0 first**: confirm from the Deco lease table that `.52` really is the charger before anyone opens a breaker panel.
- [ ] **A6 · Plug the Hub host into Ethernet** — `enp162s0` is `NO-CARRIER`; the Hub is on Wi-Fi. *Why you:* it is a cable. *Time:* minutes. *Unblocks:* the repo's own wired-preferred rule for mDNS and camera streams. Not urgent — a drifted lease now costs one Panel restart, not a re-pairing. *Measured 2026-08-04:* `/sys/class/net/enp162s0/carrier` = `0`, `nmcli` says `unavailable`, and every service the house depends on is bound `0.0.0.0` and reachable only via `wlp164s0` (`192.168.68.81/22`) — HA `:8123` and go2rtc `:1984` both answer 200 there. The NIC is a Killer E3000 advertising 2500baseT; the cable is the only missing part. → [Ch. 1 §1.2](appliance/commissioning/01-host-and-network.md) — **describes the situation and the reasoning; there is no procedure, because it is a cable.**
- [ ] **A7 · Spike day — Flutter under cage on the real touchscreen** — cage launched from a local TTY with `gdm` stopped, 13-item pass/fail battery done hands-on-glass. *Why you:* tap, two-finger, fling and pinch cannot be tested remotely. *Time:* a full work session (timeboxed at 1 day). *Gated on:* **G6** (cage is not installed) **+ a touchscreen physically attached** — **no longer on G4**, which is done: the release bundle builds, runs, and has been driven headless. *Unblocks:* the entire kiosk track and the go/no-go on native Flutter vs. the web-kiosk fallback (ADR-0001). *Measured 2026-08-04, and this is the whole gate:* (a) `cage` **not installed** — `apt-cache policy cage` → `Installed: (none) / Candidate: 0.2.1-1`; nor `libwlroots-0.19` (cand. 0.19.2-1) nor `seatd` (cand. 0.9.2-2). That is **G6**. (b) **No touchscreen is attached to this box.** `/proc/bus/input/devices` lists 23 devices — buttons, two keyboards, audio jacks, Video Bus, and a `PNP0C50:00 06CB:CFAC` **Touchpad**. There is no absolute-position or multitouch device and no `*-touchscreen` entry under `/dev/input/by-path`. If the spike screen is "on hand", it is not plugged in. (c) `gdm`/`gdm3`/`display-manager` are all `active` and the current session is a Wayland `seat0` login — stopping it needs sudo and kills the desktop. → [`appliance/README.md` "Spike-day order of operations"](appliance/README.md) → [`docs/research/flutter-cage-spike.md`](docs/research/flutter-cage-spike.md) — 10-step runbook, verbatim systemd/PAM units, 5-rung fallback ladder. **Caveat, now confirmed by measurement:** the research was written against cage 0.1.5 / wlroots 0.17; 26.04 ships **0.2.1 / 0.19.2**. The re-audit is agent work — **done 2026-08-05**: every load-bearing claim held (touch path, both `WLR_*` env names, still no output-power management so the screen-power ladder stands, the unit clean under systemd 259), recorded as dated amendments in the runbook. Spike day can run it as written.
- [ ] **A8 · The doorbell four-press protocol** — four physical presses of the Ring button, two of them staged around an HA restart. *Why you:* a finger on the physical button; everything else is watchable from a terminal. *Time:* ~15 min, with an agent (or Developer Tools → States) watching logs between presses. *Unblocks:* closing phase-7 §A — `event.front_door_ding` was minted 2026-08-05 and `doorbell` binds it, but **the entity has never seen a real press**, so the two press-loss bugs it fixes are corrected by construction, not by observation. The four presses, in order: **(1) plain press** → the Panel rings and the Popup opens unprompted; **(2) the gap press** — restart HA (`docker compose restart homeassistant`, or ask an agent), wait for reconnect, press once → **must ring** (this press was silently lost under the old word shape); **(3) the double press** — press twice ~10 s apart → **both ring** (the second was invisible before); **(4) the quiet restart** — restart HA with nobody at the door, watch 3 min → **nothing rings**. The greppable verdicts are `ui.ding` for a ring and `reason=already_rung` / `reason=stale` for the silences — not `unchanged`, which only covers same-run re-reports. Also re-run phase-3's check while you're there: a ding still arrives when **no** stream is open. → [phase-7 §A4](docs/plans/device-integrations/phase-7-doorbell-events-and-cameras.md) has the full protocol and why each press matters.
- [ ] **A9 · Drive the Cameras view by hand and watch go2rtc's counters** — the live half of phase-7 §B's "done when", and it does **not** need the touchscreen: a windowed Panel on the Hub host and a mouse are enough. *Why you:* clicks in a GUI no agent session can reach. *Time:* ~10 min. From `panel/`: `GO2RTC_URL=http://127.0.0.1:1984 HUB=ha HA_URL=http://127.0.0.1:8123 HA_TOKEN=$(cat ../hub/token) flutter run -d linux`, then: tap the right-edge camera tab → the Cameras view slides out; the doorbell tile shows a **snapshot, not live** (that is the #177014 safety working); tap it → `LIVE`, and at `http://127.0.0.1:1984` the `ring_doorbell` stream gains a consumer; tap again or close the view → **every stream back to bare `url` stubs, `consumers: []`**. The hermetic tests already prove the lifecycle; this is the observation that go2rtc agrees. *Gated on nothing.* The touchscreen version of the same check folds into **A7** whenever spike day happens. → [phase-7 Done-when](docs/plans/device-integrations/phase-7-doorbell-events-and-cameras.md).

### Accounts, credentials and vendor apps — one laptop-and-phone session

- [x] **B2 · Authenticate ring-mqtt — DONE 2026-08-05.** The owner completed the Ring email/password + 2FA flow at `http://192.168.68.81:55123/`. *Verified by:* **15 mqtt-platform entities** in the registry where there were 0, on a device named "Front Door" — and then the part that matters, an end-to-end pull of the real camera: `GET /api/stream.mjpeg?src=ring_doorbell` returned **5,330,367 bytes containing 54 JPEG frames in 8 s**. That is a live picture of the front door, transcoded by go2rtc, over the exact endpoint the appliance build consumes. *Landed the same day:* the `ring_doorbell` stream in `hub/go2rtc/go2rtc.yaml` (`rtsp://127.0.0.1:8556/90486cf35236_live` + an mjpeg producer), and the `doorbell` Key rebound off the fake `sensor.ring_doorbell` onto the real entity with `stream: ring_doorbell`. **So the Panel's video path is no longer synthetic at any point.** *The one thing that did not go as planned:* this ring-mqtt publishes **no `event.*` entity at all** — the standing advice was "prefer `event.*_ding` if it offers both", and it offers one. The doorbell is bound to `binary_sensor.front_door_ding`, which is `classifyDing()`'s word shape, and that costs a press the Panel is hearing about for the first time after a gap (rule 3; the binding comments carry the full cost). A `lastDingTime` attribute on that same entity could buy the timestamp shape back via a template sensor — **not wired**, because whether ring-mqtt updates it at the instant of the ding is UNVERIFIED and only a real press settles it. **→ Superseded 2026-08-05 (phase 7 §A):** the missing `event.*` entity was **minted** — an HA MQTT-event entity over ring-mqtt's own ding topic (`hub/ha-config/mqtt.yaml`, gitignored, tracked example beside it) — and `doorbell` now binds `event.front_door_ding`, so `classifyDing()` takes the timestamp shape after all; research settled `lastDingTime` (written at push time) and buried the template idea (Ch. 5 §1.3). Verification needs the four-press protocol (phase-7 §A4), which is owner work at the door. *#177014 measured, not assumed:* pulling the stream flipped `switch.front_door_live_stream` to `on`, and it was back `off` with `consumers: []` **within 5 s** of the pull ending. → [Ch. 5 §1.1](appliance/commissioning/05-devices-cloud.md).
- [x] **B4 · Install HACS, then complete its GitHub device-code auth — DONE 2026-08-07.** *Verified by:* `config_entries/get` against **production** (port 8123) — entry `01KZDDD6TH4CKTE9FZT1XH102B`, domain `hacs`, `state: loaded`; `update.hacs_update` entity present (only gets created once the integration's coordinator actually initializes, not from a stub entry); no errors or crash-loop in the log, just the one expected "untested custom integration" warning from restart. Install (agent work, no credentials) and device-code auth (owner, GitHub device-login page) both done, in that order. **The `hub/custom_components/hacs/` tree is now untracked in git** — a real diff, not yet decided: gitignore it or vendor it, and nothing has been staged either way. *Unblocked:* **B6, B7, B8** (all HACS-only). → [Ch. 5 §3.1–3.2](appliance/commissioning/05-devices-cloud.md) for the verified steps and exact screen text.
- [x] **B1 · Add the Ecobee cloud entry — DONE 2026-08-07.** *Verified by:* WebSocket `config_entries/get` against the **production** Hub (port 8123) — entry `01KZDCDZV2F5Q1GMZFWSSWNJVC`, domain `ecobee`, `source: homekit` (the correct branded card, not the legacy key path), `state: loaded`. Entities landed exactly as predicted: `climate.main_floor_2` (the `_2` because local already owns `climate.main_floor` — §4.2.5's collision, called in advance, not a mistake to fix), plus `weather.main_floor`, `notify.main_floor`, `number.main_floor_fan_minimum_on_time`, `sensor.main_floor_temperature`, `sensor.main_floor_humidity`, and `_2`-suffixed collisions on the bridged sensor's occupancy/temperature entities. **One real wrong turn on the way, worth keeping for the next credential flow:** the first successful login (username + password + MFA, all correct) landed on `hub/dev`'s Home Assistant (port 18123, the disposable Panel-development sandbox) rather than production (port 8123) — same "ecobee" card, same steps, wrong HA instance, because `hub/dev` runs its own independent Home Assistant with its own config-entry list. The tell was checking `config_entries/get` against the right container by name (`homeassistant`, not `homeassistant-dev`) rather than trusting what the browser tab showed. → [Ch. 4 §4.2.3](appliance/commissioning/04-devices-local.md) for the credential flow (now with a UI walkthrough, not just headless curl), [Ch. 4 §4.2.1](appliance/commissioning/04-devices-local.md) for the double-discovery trap this shares with the Rachio.
- [ ] **B5 · LG ThinQ Personal Access Token** — created at `connect-pat.lgthinq.com` signed in as the LG account the appliances are registered to. *Time:* ~15 min. *Unblocks:* the `washer` and `dryer` Keys. *Measured 2026-08-04:* no `lg_thinq` entry, component or entity. → [Ch. 5 §4](appliance/commissioning/05-devices-cloud.md) — steps, plus the caveats that matter (PAT lifetime UNVERIFIED — record the issue date; do not build a Panel affordance on remote start).
- [ ] **B6 · Whisker / Litter-Robot account credentials** — the app account's email + password. *Time:* ~15 min. *Needs:* B4 — which is further back than this list used to say. *Unblocks:* the `litter-robot` Key. *Measured 2026-08-04:* no `litterrobot` entry, component or entity. → [Ch. 5 §5](appliance/commissioning/05-devices-cloud.md) — steps. The LR5 Pro camera is not exposed in HA; do not plan it on the Panel.
- [ ] **B7 · Create a second, dedicated Petlibro account and share the feeders to it** — *Why you:* Petlibro allows one login per account, so signing HA in as the family account logs your phone out. Account work only you can do, and it must happen **before** touching HA. *Time:* ~20 min. *Needs:* B4. *Unblocks:* the `feeder-petlibro` and `feeder-granary` Keys. *Measured 2026-08-04:* no `petlibro` entry, component or entity. → [Ch. 5 §6](appliance/commissioning/05-devices-cloud.md) — steps, including the fallback of adding `jjjonesjr33/petlibro` as a custom HACS repository.
- [ ] **B8 · Emporia app account credentials** — the account that owns the Vue 3. *Time:* ~15 min. *Needs:* B4. *Unblocks:* the `energy-monitor` Key — **and it is the only route to any power/energy data in this house**: all three Kasa plugs report `feature: TIM`, no `EMETER`. *Measured 2026-08-04:* no `emporia_vue` entry, component or entity. → [Ch. 5 §7](appliance/commissioning/05-devices-cloud.md) — steps; `magico13/ha-emporia-vue` is not in the HACS default store, add it as a custom repo.
- [x] **B3 · Identify the five Wyze units, and get the Wyze API key pair — DONE 2026-08-07.** *Verified by:* full inventory recorded in `docs/plans/device-integrations/phase-4-cameras.md`'s A1 table — name, MAC, model, and firmware for all five, obtained from the owner via the Wyze app. **All five are Wyze Cam v3** (two — Garage Door Cam, Back Yard Cam — are Floodlight bundles, extra firmware for the light controller alongside the camera's own), **all on firmware 4.36.16.7064**, which is *newer* than 4.36.16.5654, the version that promoted RTSP from beta to production on 2026-02-02. **This changes D1's premise: the flash step may not be needed at all** — check each camera's own Advanced Settings for an RTSP toggle before flashing anything; start with a plain v3 (Family Room, Living Room, or Back Yard Door Cam), not a Floodlight bundle, per the research doc's extra caution on that combination. API key pair (`WYZE_API_KEY_ID`, `WYZE_API_KEY`) obtained from `developer-api-console.wyze.com`, stored at `~/.sh_keys/wyze.env` (ADR-0010). *Deco-app naming/DHCP reservation is still open but is now **C1**, not B3* — same app session, different item. *Unblocks:* **phase 4's camera half** and **D1**. → [Ch. 5 §2.1–2.3](appliance/commissioning/05-devices-cloud.md), [phase-4-cameras.md §A1](docs/plans/device-integrations/phase-4-cameras.md).

- [ ] **F1a · Draw three new placeholder camera Keys — `cam-family`, `cam-backyard`, `cam-backyard-door`** — **NEW 2026-08-07**, owner decision on the same day: only `cam-garage`/`cam-living` have plausible existing Keys for the five real Wyze cameras identified in **B3**; the other three have nowhere to bind. Rather than wait for the real house drawing (**F1**), add three new placeholder-house Keys now, matching the existing `outlet-outdoor-a`/`b` and `light-living` precedent (D5) — a marker's *position* is cheap to fix later (`panel/HOUSE-PLAN.md` §3.3: drag, save, re-run the converter, no `bindings.yaml` change), so authoring the tag now costs nothing extra at F1 time. *Why you:* Sweet Home 3D, Mac-only, no Linux path (ADR-0005) — an agent session cannot do this step. *Time:* minutes per marker once the drawing's open. *Steps:* `tool/sh3d.sh` to open (never Sweet Home 3D directly — the Placementkey field is invisible otherwise); drag a **camera** marker into any room for each; name it what the Panel should show; **Other properties…** → Placementkey = `cam-family` / `cam-backyard` / `cam-backyard-door`; save; `python3 tool/sh3d_to_yaml.py <path>.sh3d -o assets/house/house.yaml --name "..."`, check the device count went up by 3. *Unblocks:* binding the three otherwise-homeless cameras (agent-doable once the tags exist — one `bindings.yaml` entry per tag). → [phase-4-cameras.md §A1a](docs/plans/device-integrations/phase-4-cameras.md), [`panel/HOUSE-PLAN.md`](../panel/HOUSE-PLAN.md) §3.1.
- [x] **C1 · DHCP reservations by MAC on the Deco, starting with the Hub host — DONE 2026-08-07.** All six added under `More → Advanced → Address Reservation → Add Address Reservation` — the confirmed path, found by elimination after two wrong guesses (a per-device toggle that doesn't exist on this app version, and the DHCP Server screen, which is range/gateway/DNS config only, not reservations): Hub host (`ac:45:ef:d2:e5:47` → `.81`, shown in the app as **"dmorozov-Legion-9-16IRX8"** — the laptop's real hostname, left as-is, not renamed to match this doc's convention), Garage Door Cam (`…53:25:72` → `.54`), Living Room Cam (`…49:b2:f6` → `.69`), Family Room Cam (`…8d:cc:54` → `.57`), Back Yard Cam (`…4a:95:76` → `.62`), Back Yard Door Cam (`…8e:4f:b1` → `.63`). *Spot-checked immediately after* (`ip neigh`): all five camera MACs still answering at their reserved IPs — expected regardless of whether the reservation took, since nothing forces a renewal on its own. **Real verification is passive, over the next lease cycle** (Hub host's own lease was measured at 2 h, renewing hourly) — if any of the six drift after this, the reservation didn't take; if not, in a few hours it's confirmed. → [Ch. 1 §1.2](appliance/commissioning/01-host-and-network.md) — now records the confirmed path, not a guess.

### At a terminal — needs the sudo password, or the Mac

This host has no passwordless sudo. Each of these is small; an agent session simply cannot finish any of them. Do them in one sitting.

- [x] **E7 · Back up `hub/ha-config/.storage` — DONE 2026-08-04.** The owner ran the corrected `sudo tar` procedure, confirmed the five root-only files **by name** (not by counting — the count drifts), encrypted the result and moved it off this disk. **Still outstanding: a second copy.** One archive on one drive is not a backup, and the Ring half now has something to protect — `ring-state.json` exists as of B2, which is the credential whose loss costs a live 2FA session. *Why you:* the mitigation reads root-owned files, so it needs `sudo`, and the resulting tarball holds cleartext credentials, so where it lives is your call. *Time:* minutes to run, an hour to design something durable. *Unblocks:* protection against the only genuine re-pairing cliff in the system — losing `.storage` means re-entering every credential **and a physical trip to the thermostat's touchscreen**. Do it **before** B1/B2/A2 create more pairings. *Measured 2026-08-04, **before** the backup — this is the state the item existed to fix, kept because it is the argument for the second copy too:* zero archive-shaped files in the repo (maxdepth 4) or `~` (maxdepth 3, only unrelated `Downloads/*.tar.gz`); `/media/dmorozov` and `/mnt` both empty; HA's own `backup` integration is loaded but has **never run** (`sensor.backup_backup_manager_state = idle`, last-attempted / last-successful / next-scheduled all `unknown`, no `backup` file in `.storage`, no `hub/ha-config/backups/`); and `git grep` finds a tar recipe in exactly one place, `appliance/commissioning/02-hub-stack.md`. "No phase has a backup step" is confirmed. **The sudo requirement is real and now measured precisely:** `.storage/` is `0755 root`, most files are `0644` and read fine as `dmorozov`, but **exactly 5 are `0600 root`** — `auth`, `auth_provider.homeassistant`, `core.config`, `core.uuid`, `onboarding`. An unprivileged `tar` therefore captures 24 of 29 files and **silently drops the auth store**: a dangerous half-backup that looks like a backup. **Scope corrected:** this item used to say `.storage` *and* `hub/ring-mqtt-data/`. Backing up `ring-mqtt-data/` today preserves essentially nothing — it holds `config.json` and an example, and **no `ring-state.json`** (see B2). The whole present-day payload is `.storage`; the Ring half acquires value the moment B2 lands. → [Ch. 2 §8, "The re-pairing cliff"](appliance/commissioning/02-hub-stack.md) — exact `docker compose stop` / `tar` / `start` recipe, honestly labelled as the interim manual mitigation.
- [ ] **G6 · `sudo apt install cage` (+ wlroots and seatd) on the Hub host** — **NEW 2026-08-04**, surfaced by the A7 re-audit. *Why you:* it is `apt`, and this host has no passwordless sudo. *Time:* minutes, then nothing until spike day. *Unblocks:* **A7**, which cannot start without a compositor. *Measured:* `which cage` → nothing; `dpkg -l cage` → `no packages found matching cage`; `apt-cache policy` gives `cage` **0.2.1-1** (resolute/universe), `libwlroots-0.19` **0.19.2-1**, `seatd` **0.9.2-2**, all `Installed: (none)`. Note what those version numbers mean for A7: the spike research is written against cage **0.1.5** / wlroots **0.17**, so installing the packages is the easy half and re-auditing the runbook against 0.2.1/0.19.2 is the real work — that part is agent work, **done 2026-08-05** (dated amendments in the runbook; every load-bearing claim held). The `apt install` is all that remains here. → [`docs/research/flutter-cage-spike.md`](docs/research/flutter-cage-spike.md) — the runbook that needs the re-audit; [Ch. 1 §1.5](appliance/commissioning/01-host-and-network.md) for the gated-converge pattern (`ansible-playbook … --ask-become-pass`) if you would rather the kiosk role claimed these too.
- [ ] **G7 · Restart Home Assistant once, so the committed `http:` block takes effect** — `cd hub && docker compose restart homeassistant`. **NEW 2026-08-07.** *Why you, and this one does not fit the section header:* it needs no sudo and no Mac, and an agent session can physically run it. What it needs is your say-so about when the house's Hub may blink — every automation, the Panel and the doorbell go away for the duration. *Time:* under a minute. *Unblocks:* the web Panel fetching camera stills at all; today it cannot, from this Hub. *Measured 2026-08-07:* `hub/ha-config/configuration.yaml` has carried `http: cors_allowed_origins: ["*"]` + `login_attempts_threshold: 5` since commit `61961e9` (2026-08-06), HA reads that file only at boot, and this container has run since **2026-08-05 16:42 UTC** — so the block has never been read. Probed from the devcontainer: `OPTIONS http://172.17.0.1:8123/api/camera_proxy/…` with an `Origin` header still returns **403 `CORS preflight request failed`**. Re-run that probe afterwards; a `200` carrying `Access-Control-Allow-Origin` is the confirmation. Note the same restart also arms `login_attempts_threshold`, which is the half that matters — see the chapter. → [Ch. 3 §3.10](appliance/commissioning/03-home-assistant.md).
- [ ] **G2 · Push the Mac's SSH public key to the Hub host** — `ssh-copy-id`, run **from the Mac**, a machine no agent session here can reach. *Time:* minutes. Nothing is broken — password auth works — but every Ansible converge prompts. *Measured 2026-08-04:* `~/.ssh/authorized_keys` is **exactly 0 bytes**, mode 600, untouched since Nov 20 2025. This host's own `id_ed25519{,.pub}` exist and `known_hosts` was updated Aug 3 — but nothing inbound has ever been added. → [Ch. 1 §1.3](appliance/commissioning/01-host-and-network.md) — one line.
- [x] **G3 · Rotate `MOSQUITTO_RING_PASSWORD` — DONE 2026-08-07, by an agent at the owner's explicit direction ("rotate the ring MQTT password now").** Closes the item. **Sequence:** new 29-char random value set with `mosquitto_passwd -b` against the live `mosquitto` container (`-u root`, then `chown 1883:1883`/`chmod 600` restored — `mosquitto_passwd` always rewrites as `root:root`, see §3's own warning); `mqtt_url` in `~/.sh_keys/ring-mqtt/config.json` updated to match, edited **through the already-running `ring-mqtt` container** (`docker exec ring-mqtt sed -i …`) rather than a throwaway host-facing container, since it already had `/data` mounted to the right place; `~/.sh_keys/broker-passwords.env`'s `MOSQUITTO_RING_PASSWORD` line updated too, so the durable reference copy doesn't go stale; then `docker compose restart mosquitto ring-mqtt` (plain restart, not recreate — G8 already had the mounts right). *Verified, not assumed:* mosquitto logs show `ring` re-authenticating (`New client connected … u'ring'`) after a few `not authorised` retries from ring-mqtt's own old in-flight connection during the one-second gap before its restart landed; ring-mqtt's log shows a full clean cycle after — Ring API reconnect, device discovery, go2rtc relay restart — no auth errors post-restart. The old password value is superseded everywhere it lived; the scratch file the new value was staged through was `shred -u`'d immediately after use. `MOSQUITTO_HA_PASSWORD` and `MOSQUITTO_Z2M_PASSWORD` were never exposed this way and were not rotated. → [ADR-0010](../docs/adr/0010-secrets-consolidated-outside-the-repo.md).
- [x] **G8 · Restart `mosquitto`, `ring-mqtt` and `go2rtc` to pick up ADR-0010's new bind-mount sources — DONE 2026-08-07, by the agent that made the move, not deferred to you.** **Why this one broke the usual rule** ("nothing that blinks the house happens without your say-so," same as G7): moving the secret files out of `hub/` pulled them out from under the *already-running* containers' live bind mounts immediately — `docker exec ring-mqtt ls /data` showed `config.json` gone within the same minute the `mv` ran, while the process kept serving on cached in-memory state. That's a **half-migrated, more-fragile-than-either-state** condition: an uncontrolled restart (crash, OOM, host reboot) before the compose change was applied would have come back up pointed at directories with nothing in them — mosquitto refusing to start, ring-mqtt refusing to start, go2rtc silently serving zero streams. Leaving that dangling and waiting for a G7-style go-ahead was judged worse than closing it under supervision immediately. *What actually happened, in order:* `docker compose up -d` recreated the three services using `${HOME}/.sh_keys`, which resolves against the **devcontainer's own** `/home/vscode` — not the real host's `/home/dmorozov` that dockerd (docker-outside-of-docker) actually sees — so Docker silently auto-created empty placeholder directories on the real host and both mosquitto and go2rtc came up with **no credentials at all** rather than erroring. Root-caused, fixed (`hub/.env` pins the literal host path; go2rtc's file mount became a directory mount, same reasoning as the mosquitto passwd nesting), and re-verified: mosquitto shows `ha` and `ring` both authenticated, ring-mqtt rediscovered the Front Door camera and rewrote `ring-state.json`, go2rtc loaded the real stream config. No credential values were exposed further by this — the failure mode was *empty* config, not leaked config. Full account, including the discovery that **recreating any service in `hub/compose.yaml` from inside the devcontainer is unsafe** (not new to this change — every relative bind mount is affected) and the `--project-directory`/`--env-file` fix, is in [ADR-0010's "Consequences"](../docs/adr/0010-secrets-consolidated-outside-the-repo.md). *Verify if you want to see it yourself:* `docker logs mosquitto --tail 5` should show `ha`/`ring` connections; `docker logs ring-mqtt --tail 5` should show a normal snapshot-interval loop, not a config-not-found error.
- [ ] **G5 · Standing: `sudo apt upgrade` the Docker packages deliberately** — `unattended-upgrades` allows Ubuntu origins only, so `download.docker.com` packages never auto-update, and under ADR-0008 the whole Hub *is* Docker. *Time:* minutes, recurring. Not a task — a standing obligation. *Measured 2026-08-04 — the rationale is confirmed verbatim, and there is nothing pending today.* `/etc/apt/apt.conf.d/50unattended-upgrades` allows only `${distro_id}:${distro_codename}`, `-security` and the two ESM origins (`-updates` is commented out), so Docker's origin is indeed never auto-upgraded; `20auto-upgrades` has both periodic keys at `1`. But installed **==** candidate for all six packages — `docker-ce`/`-cli`/`-rootless-extras` at `5:29.7.1-1~ubuntu.26.04~resolute`, `containerd.io` `2.2.6-…`, `docker-buildx-plugin` `0.36.0-…`, `docker-compose-plugin` `5.4.0-…` — and `apt list --upgradable` is **empty overall** against a cache refreshed 2026-08-04 18:12. If anything on this list implies a pending Docker upgrade, that is stale: keep the obligation, skip the sitting. → [Ch. 1 §1.5](appliance/commissioning/01-host-and-network.md) — the `--only-upgrade` line naming all six packages.
- [x] **G4 · Install the Flutter Linux toolchain on the Hub host — DONE 2026-08-04.** *Verified by:* `clang`, `clang++`, `cmake`, `ninja` all present (`/usr/bin/clang`, `/usr/bin/cmake`, `/usr/bin/ninja`), plus `gtk+-3.0` 3.24.52, `liblzma` 5.8.3 and `xkbcommon` 1.13.1 — and, the only proof that counts, **`flutter build linux --release` succeeds**. The bundle exists at `panel/build/linux/x64/release/bundle/panel` (23832 B, built 2026-08-04 18:16) with `libapp.so` and `libflutter_linux_gtk.so` beside it. **And then it was run.** This entry used to argue at length that video on the wall was "an argument, not an observation". It is now an observation. The **Linux release binary** was launched headless under `Xvfb :99` (1280×800×24, `LIBGL_ALWAYS_SOFTWARE=1`, llvmpipe) against the **real** Hub (`127.0.0.1:8123`) and the **real** go2rtc (`127.0.0.1:1984`). Its own log records the whole chain: `hub.connected … devices=33` → `ui.ding device=doorbell entity_state=on` → `popup.doorbell reason=ding` → `popup.stream_open name=selftest` → on close, `popup.stream_closed reason=popup_closed`. A screenshot of the app window shows the "Ring Doorbell" Popup open with a **live MJPEG test pattern rendered inside it**; go2rtc logged the consumer as `user_agent: Dart/3.12 (dart:io)` with 931 kB transferred, and reports `consumers: []` again now. So: the appliance opens its own Popup unprompted on a state change, plays video, and tears the stream down cleanly. **What this does NOT prove — all four still open:** (1) **not cage** — this was Xvfb with the X11 GDK backend and software GL; `cage` is not even installed (**G6**), so the kiosk half of **A7** is untouched; (2) **no touch input** — nothing was tapped, because no touchscreen is attached (**A7**); every interaction was a state push over HA's REST API; (3) **not a real camera** — the stream was go2rtc's synthetic `ffmpeg:selftest#video=mjpeg`, and a real Ring stream adds 2–5 s of start-up on top (**B2**/**B3**); (4) **not a real doorbell** — the ding was a fabricated `sensor.ring_doorbell` state POSTed to HA, and that entity does not exist in HA at all right now (`/api/states/sensor.ring_doorbell` → `Entity not found`). *That loose end is **closed**, 2026-08-05:* the tracked `panel/assets/house/bindings.yaml` now carries `stream: ring_doorbell` on the `doorbell` Device, pointing at a real Ring stream declared in `hub/go2rtc/go2rtc.yaml` — so a Panel built from the repo today opens that Popup with **your front door** in it, not a test pattern and not an empty rectangle. Caveats (3) and (4) above are retired with it; (1) `cage` and (2) touch input are still open. → [phase-0 open item 14](docs/plans/device-integrations/phase-0-laptop-bring-up.md) + [Ch. 1 §1.7](appliance/commissioning/01-host-and-network.md).
- [x] **G1 · `apt-mark manual linux-generic-hwe-26.04` — DONE 2026-08-04.** *Verified by:* `apt-mark showmanual` contains `linux-generic-hwe-26.04`; `apt-mark showauto` does not. *Why it mattered:* while it was auto-marked its only parent was a removable transitional shim, so one `apt autoremove` could have orphaned the running kernel stack. → [Ch. 1 §1.5, "Two things it does not do"](appliance/commissioning/01-host-and-network.md).

### Decisions only you can take

- [ ] **E1 · GNOME battery auto-suspend — accept the flat battery, or accept the 15-minute outage** — unplugging the laptop kills the whole house's Hub 15 minutes later. Turning it off removes the only thing stopping an unplugged daily-driver laptop draining flat. *Why you:* it is a power setting on your personal machine, not a technical call. *Time:* minutes; execution is one command in your own session. *Measured 2026-08-04:* unchanged and exact — `sleep-inactive-battery-type` = `'suspend'`, `sleep-inactive-battery-timeout` = `900` (the stated 15 minutes); on AC the pair is `'nothing'` / `0`. `ADP0/online` = 1 right now, so the trap is armed but not tripped. → [Ch. 1 §1.4, "GNOME auto-suspend"](appliance/commissioning/01-host-and-network.md) — the exact `gsettings set`, plus both caveats.
- [x] **E4 · Rachio's zone-run duration — DONE 2026-08-05.** Set to **5 minutes**. *Verified by:* the `rachio` entry's `options` in `.storage/core.config_entries` reads `{"manual_run_mins": 5}`. Note for whoever checks this next: the REST endpoint `/api/config/config_entries/entry` does **not** expose `options` and reports `None` — read `.storage`, or you will conclude this was never done. A mis-tap now costs 5 minutes of one zone instead of 10. **A5 is no longer gated.** *Why you:* this is the knob that decides what a mis-tap costs in water. A zone switch tapped from the Panel runs for that long, then stops. **Do it before A5.** *Time:* minutes. **Description corrected 2026-08-04 — this said "the default is short", and that adjective was doing far too much work.** The rachio entry's `options` was `{}` (nothing had ever been set) and `supports_options: true`, so the options flow existed and was empty. The effective value read out of the running image: `homeassistant/components/rachio/const.py` defines `CONF_MANUAL_RUN_MINS = "manual_run_mins"` and `DEFAULT_MANUAL_RUN_MINS = 10`, and `switch.py` applies `.get(CONF_MANUAL_RUN_MINS, DEFAULT_MANUAL_RUN_MINS)` at both call sites. **A Panel mis-tap on a zone switch today runs that zone for 10 minutes** (HA 2026.7.4). That is not short; it is the entire cost this item exists to bound. → [Ch. 4 §4.3.2, last paragraph](appliance/commissioning/04-devices-local.md) — **names the option and where it lives; the exact UI path is not quoted.**
- [ ] **E5 · `unit_system` — `metric` or `us_customary`** — a genuine household preference; every HA surface in the house follows it. *Time:* minutes. **De-escalated:** this used to be a Panel trap and is not any more — the `83.0 °C`-on-the-wall defect is fixed at the source. *Measured 2026-08-04:* currently **`us_customary`** (`temperature: °F`, `length: mi`, `mass: lb`, `volume: gal`, `pressure: psi`), `time_zone America/Los_Angeles`, `country US`, HA `2026.7.4`. The fix is confirmed downstream too: `sensor.main_floor_current_temperature` = 82.94 with `unit_of_measurement: °F`, and the release binary logged `hub.temperature_unit unit=°F` on connect. Low-stakes preference, not a blocker. → [Ch. 3 §3.1](appliance/commissioning/03-home-assistant.md) for the facts, [§3.5](appliance/commissioning/03-home-assistant.md) for the one-line `hactl` change.
- [ ] **E6 · Should this Hub have Bluetooth at all?** — mounting the host's system D-Bus socket into the HA container is a small but real increase in what that container can see. The honest alternative is to disable the entry and let the Hub stop retrying. *Why you:* a posture decision on your machine — the *execution* of either option needs no owner and no sudo. *Time:* minutes. *Unblocks:* nothing in phases 1–6 needs BLE, and the adapter will not exist on the mini PC. *Measured 2026-08-04 — it is not idling, it is burning cycles:* the entry is `state: setup_retry`, `disabled_by: null`, with the verbatim reason `hci0 (AC:45:EF:D2:E5:4B): DBus service not found; docker config may be missing `-v /run/dbus:/run/dbus:ro`: {ex}` (the un-interpolated `{ex}` is upstream's own bug). `docker inspect homeassistant` confirms only two mounts, `ha-config` and `custom_components` — **no `/run/dbus`** — and `hub/compose.yaml` has no dbus line. The log carries **115** `hci0 … Failed to force stop scanner` events between 07:16 and 01:17, one retry every ~10 minutes, indefinitely. Neither option has been applied. → [Ch. 3 §3.7](appliance/commissioning/03-home-assistant.md) — steps for both options, both UNVERIFIED, neither applied.
- [ ] **E3 · Wyze fleet flashing scope** — a fleet-wide risk decision on your own hardware: the RTSP firmware lags mainline (no further security updates) and loses some app features. Recommendation is to flash **one** v3 and decide the fleet after it has run for a few days. *Gated on:* B3. *Unblocks:* whether any camera in this house is ever `connectivity: local` — the only genuinely local outcome available in phase 4/5. → [plan D-log D1](docs/plans/device-integrations/README.md) + [Ch. 5 §2.2](appliance/commissioning/05-devices-cloud.md) — steps for the experiment; the decision gate is stated explicitly.
- [ ] **E2 · The SmartThings oven — decide before October 2026** — a recurring $4.99/mo subscription against a dated deadline, plus a Samsung account OAuth login if it goes ahead. *Time:* minutes to decide. *Unblocks:* the `oven` Key. If it goes the other way, the honest end state is the `entity:` line removed and an unknown-state pin. Worth knowing first: what you get is *monitoring plus a Stop button*, not remote cooking control. → [Ch. 5 §8](appliance/commissioning/05-devices-cloud.md) + [research §3.5](docs/research/hub-and-device-integrations.md) — **decision support, deliberately not steps** ("Do not build this").
- [x] **E8 · go2rtc's `origin: "*"` — DECIDED 2026-08-04: accepted, and landed the same day.** Nothing is left for you here; it is kept on the list because the *reasoning* is the part worth being able to re-read. go2rtc **403s any WebSocket upgrade carrying an `Origin` header** unless `api.origin` says otherwise, and a browser always sends one — so a Panel served from anywhere except `:1984` itself could never open a stream, and what it saw was a bare connection failure with no error frame to explain it. The obvious tighter alternative — name the Panel's exact origin and nothing else — was measured on the same instance and is **broken in 1.9.10**: `api.origin` set to `"http://localhost:8080"` still 403s that very origin. **There is no allowlist**; the choice was `"*"` or no video. *Why it was yours:* it widens what any page in any LAN browser can reach on an **unauthenticated** service — today one synthetic test pattern, after phase 4 every camera in the house. *The reasoning for accepting it:* **access to this system is LAN-only or over the VPN, so the network boundary is the control** (ADR-0008 — one box, no port-forward). The origin check was never what kept strangers off an unauthenticated service; declining `"*"` would have cost the Panel its picture in exchange for a protection that was not there. Revisit when a real camera stream lands, which is when the exposure stops being synthetic — and do **not** "tighten" this to a hostname later without re-measuring: it will look correct and silently break every camera. *Landed:* `api: origin: "*"` in `hub/go2rtc/go2rtc.yaml`, mirrored into the tracked `hub/go2rtc/go2rtc.example.yaml` — **re-confirmed present 2026-08-04**. Re-measured against the live 1.9.10 daemon (`revision df95ce3`): no `Origin` → `101`, `Origin: http://127.0.0.1:1984` → `101`, `Origin: http://localhost:8080` → **`101`**, where that last one was `403` before. *Unblocked:* the transport half of phase 4 §B — and as of the appliance run recorded in **G4**, that transport has now carried real pixels into the Linux release build. What still gates a picture of your house is a camera to point at (**B3**/**B2**). → [phase-4 §B0](docs/plans/device-integrations/phase-4-cameras.md).

### Purchases

- [ ] **D2 · SMLIGHT SLZB-06 Zigbee coordinator, Ethernet mode** — *Why you:* money. ADR-0003 pins the product. *Unblocks:* Zigbee2MQTT, and therefore all of **D3**. *Measured 2026-08-04 — not bought, or at least not on this network:* a ping-sweep of the entire `192.168.68.0/22` found **30 live hosts**; OUI resolution against `/usr/share/ieee-data/oui.csv` shows TP-Link ×5, Wyze ×5, Espressif ×2, AMPAK ×2, Ring, ecobee, Chamberlain, plus Samsung/Roku/Intel/Murata/Liteon and 3 randomized-MAC phones — **no SMLIGHT device** (that OUI file has no SMLIGHT registration at all, so the fallback test was behavioural: the SLZB-06's factory Zigbee port **6638** is closed on all 30 hosts, including both Espressif candidates). Repo state agrees — `hub/z2m-data/` holds only `configuration.example.yaml`, so the `SLZB-06-IP-OR-HOSTNAME` placeholder has never been filled in and Zigbee2MQTT stays parked behind the `zigbee` profile. → [Ch. 2 §7](appliance/commissioning/02-hub-stack.md) — steps for the bring-up once the hardware exists (copy the example config, set IP + port 6638, `docker compose --profile zigbee up -d`, then pin the exact image tag).
- [ ] **D1 · ratgdo32 board for the myQ garage opener** — *Why you:* money, plus a compatibility check only you can make: read the learn-button colour on the physical opener (Security+ 2.0 = yellow; 1.0 has a wall-panel caveat) **before ordering**. Chamberlain's third-party API shutdown is permanent and HA dropped the integration in 2023.12 — **there is no software-side move at all.** *Effort:* a purchase, then 3 wires into the wall-control terminals + an ESPHome flash. *Unblocks:* the `garage-door` Key, the only Key in the house that is togglable and has no hardware. *Measured 2026-08-04:* a Chamberlain-OUI host is on the LAN at `192.168.68.53`, which is the opener itself — it is not, and cannot become, an HA entity without this board. → [research §3.11](docs/research/hub-and-device-integrations.md) — **covers the product decision thoroughly (ratgdo32 ≈ $45, Konnected blaQ as the alternative) but contains no install procedure; no commissioning chapter covers ratgdo yet.**
- [ ] **D3 · First batch of Zigbee switches/outlets, and the in-wall mains work** — *Why you:* money, and mains wiring inside walls. *Gated on:* D2. *Unblocks:* every `light-*` Key — 13 of the 33 Keys are lights, all currently on dev stand-ins. *Corroborated 2026-08-04:* the release binary's own startup log lists 29 of 33 Devices as missing entities, and the named ones are exactly the stand-ins (`input_boolean.light_family`, `input_boolean.light_reading`, …). → [research §4](docs/research/hub-and-device-integrations.md) and "New lighting/outlets" in [docs/roadmap.md](docs/roadmap.md) — **product guidance with citations, not a procedure.**
- [ ] **D5 · The Ryzen AI mini PC** — *Why you:* money, plus criteria only you can check at the point of sale: a free SO-DIMM slot and **2 × M.2** (future RAM for a local LLM, second disk for NVR, Hailo-8L accelerator). *Blocks nothing today* — ADR-0008 puts the Hub on the laptop. Remember it keeps Ubuntu Server **24.04** + HWE, not 26.04. → "Mini-PC" in [docs/roadmap.md](docs/roadmap.md) + [`docs/research/frigate-amd-acceleration.md`](docs/research/frigate-amd-acceleration.md) — **criteria, not steps.**
- [ ] **D4 · Install the two Kasa Wi-Fi wall switches** — in-wall mains work: breaker off, verify dead, neutral required. *Unblocks:* **nothing.** Worth stating plainly — the plan opened believing phase 2 depended on these; it does not, and phase 2 was rewritten to drop the dependency. *Measured 2026-08-04:* HA holds exactly three `tplink` entries and all three are plugs — `TP-LINK_Smart Plug_722C EP40`, `Old fridge HS103`, `Aquarium HS103`. No Kasa wall switch is on the LAN at all. → [`appliance/COMMISSIONING.md`](appliance/COMMISSIONING.md), "Things that exist only on the hardware", the Kasa wall-switch row — **it describes the situation and says outright that no chapter covers the install.** There is no procedure to follow.
- [ ] **D6 · (Deferred) Final production touchscreen** — criteria to verify per candidate panel: DDC/CI support (cheap HDMI touchscreens frequently do not implement it — make it a purchase criterion), firmware touch-slot count ≥ 5, USB HID quirks, HDMI-CEC. Not yet due; the spike is supposed to characterise the screen already on hand. **Flagged 2026-08-04:** that screen is **not attached to this box** — the kernel's input list has no touchscreen of any kind (see **A7**). Before spike day, confirm the spike screen physically exists; if it does not, this deferred purchase quietly becomes A7's real blocker. → [`docs/research/flutter-cage-spike.md` §6](docs/research/flutter-cage-spike.md) — **names the criteria; no procedure.**

### Drawing work — Sweet Home 3D, on the Mac

- [ ] **F1 · Draw the real house, and author the Keys it unblocks** — *Why you:* it is your house, and there is **no Linux path** (`panel/tool/sh3d.sh` hard-codes the macOS bundle, and the launcher is mandatory — without it the `Placementkey` field is invisible). Under ADR-0005 a Key is authored in the drawing, never in `bindings.yaml`, and the loader enforces that fatally in both directions. *Measured 2026-08-04, from the release binary's own startup log:* `house.loaded name="Demo House" floors=3 rooms=15 devices=33 bound=33 streams=1` — what ships today is still generated from the placeholder. Binding real hardware to placeholder Keys is fine; believing the Key names describe your house is not. *Effort:* a work session, probably several, plus `python3 tool/sh3d_to_yaml.py` and `dart run tool/gen_dev_entities.dart`. → [`panel/HOUSE-PLAN.md`](panel/HOUSE-PLAN.md) — the best start-to-finish document in this repo for a non-programmer, including every error message and what to do about it; [Ch. 6 §6.2](appliance/commissioning/06-panel-and-bindings.md) for the exact command sequence.
  Four things are queued behind that one session:
  - ~~Kitchen outlet Keys for **"Old fridge"** and **"Aquarium"**~~ — **dropped 2026-08-05, and not by doing the work.** Both plugs were repurposed: the fridge plug now drives the **entry light**, the aquarium plug the **stairs light**. That deleted the requirement rather than satisfying it — as lights they need no *outlet* Key, and the placeholder house already carries 13 `light` Keys, so this stopped being drawing work at all. Both are bound and live on the Panel today — `switch.entry_light` → `light-living`, `switch.stairs_light` → `light-landing`, placeholder Key names that F1 re-keys (§4.1.1b explains why not `light-hall`: it is the test suite's own fixture, and binding it turns 8 tests red). The 2026-08-04 decision to accept ADR-0006's one-tap consequence on a fridge is moot rather than reversed: the hazard is gone because the load changed. → [Ch. 4 §4.1.1](appliance/commissioning/04-devices-local.md).
  - The outlet arithmetic: **2** live switchable outlet sockets against 3 `outlet` Keys — the EP40's two children take `outlet-outdoor-a`/`-b` and `outlet-master` is spare. (Was "3 Keys, 4 sockets, two new Keys needed"; the other two sockets are lights now.) **No new outlet Keys needed.**
  - Camera Keys: three `cam-*` exist against up to five Wyze units. If four or five are worth showing, the extras have nowhere to bind — plan the session, or decide deliberately that two stay off the Panel.
  - A Key for the Ecobee's bridged room sensor — `sensor.family_room_temperature` (83.84 °F) / `binary_sensor.family_room_occupancy` / battery (100 %) arrived over the **local** path, unasked-for, and are live right now.
- [ ] **F2 · An irrigation Key for the Rachio — drawing *and* code** — *Why it is separate:* `panel/lib/domain/device_vocabulary.dart` carries 14 `DeviceKind` values and none of them is irrigation, so a Rachio pin is a code change (new `DeviceKind` + `KindSpec` + seed, rippling through the loader, `FakeHub`, the state fold and the dev-entity generator) **and then** a drawing session. Forcing a zone into `outlet` would inherit single-tap togglability (ADR-0006) for something that should not be tapped by accident — and **E4** now puts a number on that: 10 minutes of water per accident. *Your half:* the drawing, and the product decision that irrigation belongs on the wall at all. The code half is agent work. *Unblocks:* the 5 loaded Rachio zone switches — verified live, with `Zone number` 1–5 — currently have nowhere to land on the Panel. → [Ch. 4 §4.3.6](appliance/commissioning/04-devices-local.md) + [phase-2 §4c](docs/plans/device-integrations/phase-2-local-quick-wins.md) — **describes the two-part change clearly; there is no step-by-step for the vocabulary edit** beyond the doc comment in `device_vocabulary.dart`, which lists what ripples.

## Calendar items (from research — dated risks)

- ~~**Before HA 2026.8**: define explicit `turn_on`/WOL actions per Samsung TV~~ — **closed 2026-08-03**: Samsung TVs are out of scope by owner decision, and HA is pinned at `2026.7`, which is the guard. The pending `samsungtv` zeroconf card on the Hub is dismissible clutter, not work.
- **By October 2026**: decide — pay Samsung's $4.99/mo SmartThings API "Personal Plan" or drop the oven integration. → **E2** above.
- **Known issue**: HA core #177014 — open Ring live streams can suppress doorbell events; open streams on demand only. **Enforced in the Panel since 2026-08-04**: the Popup opens a stream when it opens and closes it in `dispose()` (which runs even when the route leaves without a pop), nothing holds one in the background, and the Popup a ding opens closes itself after 30 s rather than leaving a session up for nobody.
